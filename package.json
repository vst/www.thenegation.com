{
  "name": "thenegation",
  "version": "2.1.0",
  "description": "vst's Personal Website",
  "author": "Vehbi Sinan Tunalioglu",
  "license": "CC-BY-SA-4.0",
  "homepage": "https://github.com/vst/www.thenegation.com#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vst/www.thenegation.com.git"
  },
  "bugs": {
    "url": "https://github.com/vst/www.thenegation.com/issues"
  },
  "private": true,
  "scripts": {
    "dev": "hugo server",
    "format:prettier": "prettier --write .",
    "format:taplo": "taplo fmt",
    "format": "pnpm run format:prettier && pnpm run format:taplo",
    "lint:taplo": "taplo check",
    "lint": "pnpm run lint:taplo",
    "check:format:prettier": "prettier --check .",
    "check:format:taplo": "taplo fmt --check",
    "check:format": "pnpm run check:format:prettier && pnpm run check:format:taplo",
    "check": "pnpm run check:format && pnpm run lint && pnpm run build",
    "build:hugo": "hugo build --gc --minify",
    "build:pagefind": "pnpm dlx pagefind --site public",
    "build": "pnpm run build:hugo && pnpm run build:pagefind"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "@tailwindcss/typography": "^0.5.20",
    "prettier": "^3.9.5",
    "prettier-plugin-go-template": "^0.0.15",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "tailwindcss": "^4.3.2"
  },
  "prettier": {
    "printWidth": 80,
    "proseWrap": "always",
    "plugins": [
      "prettier-plugin-go-template",
      "prettier-plugin-tailwindcss"
    ],
    "overrides": [
      {
        "files": [
          "*.html"
        ],
        "options": {
          "parser": "go-template",
          "goTemplateBracketSpacing": true,
          "bracketSameLine": true
        }
      },
      {
        "files": [
          "*.js",
          "*.ts"
        ],
        "options": {
          "useTabs": false,
          "singleQuote": false
        }
      }
    ]
  }
}
